/**
 * @file
 * Test task theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.testTaskTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
